# دليل البحث عن عقد عمل في البرتغال من الجزائر

واجهة معلوماتية تفاعلية مبنية بـ Next.js وTailwind CSS، تساعد الباحثين عن عمل في الجزائر على فهم المسار الكامل للحصول على عقد عمل في البرتغال: من تجهيز الوثائق إلى التقديم على التأشيرة، مع التركيز على القطاعات المطلوبة والمصادر الموثوقة.

## 🚀 المتطلبات

- Node.js 18 أو أحدث
- npm

## ▶️ التشغيل محليًا

```bash
npm install
npm run dev
```

ثم افتح http://localhost:3000 في المتصفح.

## 📂 البنية

```
app/           صفحات الواجهة (App Router)
components/    مكونات واجهة قابلة لإعادة الاستخدام
lib/           بيانات ومساعدات مشتركة
```

## 📦 سكربتات npm

- `npm run dev` تشغيل الخادم التطويري.
- `npm run build` إنشاء نسخة الإنتاج.
- `npm start` تشغيل خادم الإنتاج بعد البناء.
- `npm run lint` التحقق من الجودة عبر ESLint.

## 🧭 الميزات

- محتوى مُبسّط باللغة العربية باتجاه RTL.
- مخطط زمني للخطوات الأساسية للحصول على عقد.
- عرض لأهم القطاعات البرتغالية التي تطلب عمالة من الخارج.
- قائمة مصادر وروابط رسمية موثوقة.
- قسم أسئلة شائعة للإجابة عن أهم الاستفسارات.
